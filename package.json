{
  "name": "@rsquad/ton-utils",
  "version": "1.3.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "release": "npm run build && standard-version",
    "release:patch": "npm run build && npm run release -- --release-as patch",
    "release:minor": "npm run build && npm run release -- --release-as minor",
    "release:major": "npm run build && npm run release -- --release-as major"
  },
  "devDependencies": {
    "@types/node": "^16.4.7",
    "standard-version": "^9.3.1"
  },
  "dependencies": {
    "@tonclient/core": "^1.20.0",
    "@tonclient/lib-node": "^1.20.0",
    "typescript": "^4.3.5"
  }
}
